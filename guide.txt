Create the article using the topic, style, and requirements I provide.

Structure and formatting:

Return valid HTML.
Use <article class="article-page">.
Use one <h1> for the title.
Use <h2> for main sections and <h3> for subsections when needed.
Use <p> for paragraphs.
Use <ul> / <ol> for lists when appropriate.
Use <blockquote> for quotations when appropriate.
Use <a> for links.
Use <img> with descriptive alt text for images.
Keep the HTML clean and semantic.
Do not include CSS or JavaScript.
Do not add <html>, <head>, <body>, header, or footer unless I explicitly request them.
